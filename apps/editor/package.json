{
  "name": "@breeze/editor",
  "version": "0.64.2",
  "private": true,
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@breeze/runtime": "workspace:*",
    "@breeze/schema": "workspace:*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-moveable": "^0.56.0",
    "zustand": "^5.0.2"
  },
  "//gsap": "Kept only so Vite can resolve the alias target and the types when it builds @breeze/runtime's dist. Nothing under src/ imports GSAP directly. Keep the ~3.15.0 pin equal to packages/runtime's — two different resolutions in one workspace would give the editor preview and the /play page different animation engines, which is precisely the drift ROADMAP rule 1 exists to prevent.",
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "gsap": "~3.15.0",
    "happy-dom": "^20.11.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
